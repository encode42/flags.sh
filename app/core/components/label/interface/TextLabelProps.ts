import LabelProps from "./LabelProps";

/**
 * Properties of a TextLabel
 */
export default interface TextLabelProps extends LabelProps {
    /**
     * The text label that will be displayed above the input.
     */
    "label": string
};
